
const options = {
    title: {
        text: "Mitt diagram"
    },
    chart: {
        renderTo: "container",
        type: "column"
    },
    series: [
        {
            
        }
    ]
};

const chart = new Highcharts.Chart(options);